# Core dependencies
torch>=2.0.0
transformers>=4.36.0
peft>=0.7.0
datasets>=2.15.0
accelerate>=0.25.0

# Optional dependencies for better performance
# flash-attn>=2.0.0  # For faster attention computation
# bitsandbytes>=0.41.0  # For quantization support

# Development and utilities
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Logging and monitoring
wandb>=0.16.0  # Optional: for experiment tracking
tensorboard>=2.15.0  # Optional: for tensorboard logging